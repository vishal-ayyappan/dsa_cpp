#include <bits/stdc++.h>
using namespace std;



int distinctSubsequences(string s) {
    //Get all the subsets, and store it in the set
    int n = s.length();
    vector<int> dp (n);
    // dp[i] --> no of subsequences from 0 to i
    getDistinctSubseq()

}